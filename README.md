# Electron demo

## Getting Started

```no-highlight
$ npm init
$ npm install electron --save-dev
```

* Create `index.js`
* Create `index.html`
* Add 'start' command to `package.json`
* Run `$ npm start`
